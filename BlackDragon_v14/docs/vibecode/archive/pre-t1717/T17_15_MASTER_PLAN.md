# T17.15 Master Plan

1. SCAN/RRI: bind the 223 deferred log candidates to the state-only policy and lock the numeric cap/refresh oracle.
2. RED: add capability, hard-cap and stateful ladder regression tests; prove the source contract fails on the baseline.
3. BUILD: add a read-only pending capability, route Overlap through the capability policy, preflight the projected cap, then refresh/persist the BUILDING target post-trim.
4. VERIFY: run focused and full Linux model/source gates, exact-head Windows MetaEditor compile, and the 23-suite native matrix.
5. EVIDENCE/RETRO: bind HEAD/TREE/EX5 hashes, keep PR Draft and hand the build to the owner for the failing Strategy Tester replay.

Stop on an ambiguous execution outcome, cap violation, target persistence failure, source/model/native regression, or PR-head drift.
