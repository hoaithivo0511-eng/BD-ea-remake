# T17.19 Master Plan

Baseline: remote `b971f41fc92ac8b084ccfa6748a59ae166d5f939`, tree
`6da21b0dbaeda85ce66f12825064521be40e75c5`.

1. SCAN — trace the supplied tester log to terminal `MAXED_NO_HEDGE`, continued Core DCA and margin failure.
2. SPECIFY/DECIDE — lock exact positive-close ownership, aggregate cash, two-stage reset/return geometry, outer cap and the owner-amended DCA-only wait gate.
3. RED — add an independent pure C++ oracle, native MQL suite and source contract before runtime code.
4. BUILD — add separate atomic re-entry persistence, exact close collection, restart-safe FSM and fresh G1 handoff to the existing Hedge Pyramid ladder.
5. ADMISSION — block Core DCA in WAIT_RESET/ARMED; leave Core Pyramid ADD to existing settings; block growth but preserve Peel at EXHAUSTED.
6. VERIFY — run focused tests, all model/source/repository gates, deep review and exact branch-head Windows CI.
7. VPS — if a dedicated BlackDragon tunnel workspace is available, bind exact source hash, compile on MT5-2, launch tester and collect result/events/artifacts. Never overwrite `demo`.
8. EVIDENCE — hash artifact and EX5, verify provenance, post PR Draft handover and leave owner Strategy Tester pending unless genuine tester evidence passes.

Stop on remote movement, tree mismatch, ambiguous protective identity, persistence
failure, any compile warning/error, matrix mismatch or provenance/hash mismatch.
