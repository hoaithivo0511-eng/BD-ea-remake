# T17.16 Retro

Provisional guard promotions:

1. A staged exposure refill must prove the exact admitted stage/target; numeric progress alone is not identity.
2. Any dynamic denominator/target refresh invalidates prior admission authority before another risk mutation.
3. Broker capacity is shared across strategy modules; a module-local or one-bar `NO_MONEY` latch is insufficient.
4. Capacity recovery requires hysteresis and must not block risk-reducing actions.
5. Wide stress `.set` combinations need a visible cross-configuration warning even when each individual input is valid.

Final promotion awaits exact-head native evidence and owner Strategy Tester replay.
