# T17.19 Retro

Status: open until verification and owner tester evidence complete.

Guardrails locked before build:

- terminal positive recovery is a chain-level economic fact, not a child-deal label;
- protective ownership must be exact and persistence-backed;
- persist a retryable transition before changing the broker-facing generation book;
- a wait-state growth policy needs separate DCA and Pyramid-ADD gates;
- risk-reducing Drive/Peel must never be hidden behind an ADD gate;
- tunnel evidence is additive and must be bound to exact source bytes.

Evidence-driven additions:

- a tester process/report marked PASS is only an infrastructure/liveness fact;
  feature coverage requires the expected FSM transitions in the journal;
- `.set` string values and numeric optimizer records have different syntax;
  validate staged inputs before attributing an `OnInit` rejection to the EA;
- keep a bounded trigger scenario separate from an economic/default scenario;
  aggressive validation may time out or expose unsafe settings without proving
  a source defect;
- the tunnel's single-EA deploy cannot represent the repository include tree;
  any dependency-closure stage must remain temporary and be tied to all
  canonical source hashes, while exact-head CI remains authoritative;
- retain owner QA for restart boundaries, inverse direction and broker
  behavior even after one genuine Strategy Tester path is green.

Status remains open until exact-head CI/artifact evidence and owner acceptance
are reconciled.
